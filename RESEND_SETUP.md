# إعداد خدمة البريد الإلكتروني (Resend Email Setup)

## المشكلة
المستخدمون يتم حفظهم في قاعدة البيانات لكن **لا يتلقون رسائل تأكيد البريد الإلكتروني**.

## الحل
استخدام **Resend** - خدمة بريد مجانية وموثوقة

## خطوات الإعداد

### 1. إنشء حساب Resend مجاني
1. اذهب إلى [resend.com](https://resend.com)
2. انقر على "Sign Up" وأنشئ حساباً مجانياً
3. تحقق من بريدك الإلكتروني

### 2. الحصول على API Key
1. بعد تسجيل الدخول، اذهب إلى **API Keys** (في اللوحة الجانبية)
2. انسخ **API Key** (يبدأ بـ `re_`)

### 3. إضافة المتغيرات إلى البيئة

**في ملف `.env.local` (للتطوير المحلي):**
```
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxx
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

**في Vercel Project Settings (للإنتاج):**
1. اذهب إلى [vercel.com/dashboard](https://vercel.com/dashboard)
2. اختر مشروعك
3. اذهب إلى **Settings → Environment Variables**
4. أضف:
   - `RESEND_API_KEY`: قيمتك من Resend
   - `NEXT_PUBLIC_APP_URL`: عنوان موقعك (مثل: `https://yourdomain.com`)

### 4. اختبار الإعداد
1. أعد تشغيل خادم التطوير
2. سجل مستخدم جديد
3. يجب أن تتلقى رسالة بريد تأكيد من `noreply@example.com`

## ملاحظات مهمة

- **البريد الاختباري**: في البداية، Resend يسمح فقط بإرسال البريد إلى بريد المشروع أو البريد الموثق
- **تفعيل النطاق**: لإرسال البريد إلى أي بريد، قم بتوثيق النطاق الخاص بك في Resend
- **مجاني**: Resend مجاني تماماً للاستخدام المنخفض

## استكشاف الأخطاء

**إذا لم تصل الرسائل:**
1. تحقق من `RESEND_API_KEY` في المتغيرات البيئية
2. افحص سجل الخوادم (Console) للأخطاء
3. تأكد من إضافة `NEXT_PUBLIC_APP_URL` بشكل صحيح

## الملفات المعدّلة
- `app/api/auth/signup/route.ts` - أضيف إرسال البريد
- `package.json` - أضيف مكتبة `resend`
- `.env.local` - أضفت متغيرات البيئة
